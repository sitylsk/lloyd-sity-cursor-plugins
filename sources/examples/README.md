# Example library run

These files are a copy of a real `/save-source` → `/draft-from-sources` → `/cite-check` pass (2026-09-17).

1. Saved three public pages: Cursor Plugins docs, Plugins reference, and `cursor/plugin-template`.
2. Drafted `drafts/what-belongs-in-a-cursor-plugin.md` using only those ids.
3. Cite-check verdict: **pass** (`CITE_CHECK.md`).

In a normal workspace the same files land under `.cursor/sources/`. This folder is committed so the format is visible in the repo.
